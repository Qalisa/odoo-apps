<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vue du formulaire des contacts (res.partner) -->
    <record id="view_partner_form_inherit_custom_fields" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.custom.fields</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='contact_addresses']" position="after">
                <page string="Informations Supplémentaires" name="additional_info">
                    <group>
                        <field name="birthdate"/>
                        <field name="birthplace"/>
                        <field name="national_id_card_number"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

    <!-- Vue du formulaire des factures (account.move) -->
    <record id="view_move_form_inherit_custom_fields" model="ir.ui.view">
        <field name="name">account.move.form.inherit.custom.fields</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <group name="partner_additional_info" string="Informations complémentaires">
                    <field name="partner_birthdate" invisible="not partner_birthdate"/>
                    <field name="partner_birthplace" invisible="not partner_birthplace"/>
                    <field name="partner_national_id_card_number" invisible="not partner_national_id_card_number"/>
                </group>
            </xpath>
        </field>
    </record>

    <!-- Vue du formulaire des commandes (sale.order) -->
    <record id="view_order_form_inherit_custom_fields" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.custom.fields</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <group name="partner_additional_info" string="Informations complémentaires">
                    <field name="partner_birthdate" invisible="not partner_birthdate"/>
                    <field name="partner_birthplace" invisible="not partner_birthplace"/>
                    <field name="partner_national_id_card_number" invisible="not partner_national_id_card_number"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>